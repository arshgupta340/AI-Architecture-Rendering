# T12 — Update spike/requirements.txt with new test/schema deps

- **Task:** Add `pytest`, `respx`, `pydantic` (for T13) to `spike/requirements.txt` with minor-version pins; verify venv already has them; do not run pip.
- **Commit:** (set after commit lands)
- **Started:** 2026-05-18T00:00:00Z
- **Finished:** 2026-05-18T00:00:00Z
- **Status:** done

## What was done
Added three new dependency lines to `spike/requirements.txt` covering the test/schema packages introduced by earlier overnight tasks (T11 brought in `pytest` + `respx`; T13 will need `pydantic`). All three packages were already installed in the venv at `spike/.venv/` so no `# install:` comment was needed. Minor-version pins were chosen to match the versions already present (pytest 9.0.3, respx 0.23.1, pydantic 2.13.4) using `>=` on the minor version, consistent with the existing style in this file.

## Files changed
- `spike/requirements.txt` — added `pytest>=9.0`, `respx>=0.23`, `pydantic>=2.13` under a new "Test + schema deps" section with a comment noting verification date.

## Checks run
- `./spike/.venv/Scripts/python.exe -c "import pytest, respx, pydantic; ..."` → pytest 9.0.3, respx 0.23.1, pydantic 2.13.4 — all present, no pip needed.

## Surprises / notes for the human
- Existing pins in the file all use `>=` rather than `==`. I followed that style. If you want hard pins for reproducibility, switch them all to `==` together.
- No `# install:` comment was added because the venv already had every package. If the venv ever gets rebuilt, `pip install -r spike/requirements.txt` should restore everything.

## Follow-ups
- T13 will start consuming `pydantic` for the `BBox`/`Region`/`TagRegionsResponse` schemas; the import should now be covered by the manifest.
