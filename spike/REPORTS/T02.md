# T02 — Renderer-package skeleton

- **Task:** Create `spike/renderers/__init__.py` exporting `Renderer` base class, and `spike/renderers/base.py` defining `class Renderer(ABC)` with abstract `render(screenshot_path, prompt, *, seed=None, **kwargs) -> bytes`. Include `name`, `provider`, `cost_per_call_usd`, `env_var` ClassVars for the bake-off scoring.
- **Commit:** 518bdcd
- **Started:** 2026-05-17 (main-session smoke test, not spike-builder agent)
- **Finished:** 2026-05-17
- **Status:** done

## What was done

Created `spike/renderers/` as a proper Python package with two files:

1. `__init__.py` — module docstring describing the package's role (Spike 2.5/B3 candidate clients, all env-gated, lazy credential reading) and re-export of `Renderer`.
2. `base.py` — abstract `Renderer` class with four ClassVars (`name`, `provider`, `cost_per_call_usd`, `env_var`) and one abstract method `render(screenshot_path, prompt, *, seed=None, **kwargs) -> bytes`. The contract requires subclasses to raise `RuntimeError` when `env_var` is unset.

The ClassVar layout (especially `env_var` and `cost_per_call_usd`) is what `compare_renderers.py` (T08) will introspect to decide which renderers to instantiate and to compute total cost projections.

## Files changed

- `spike/renderers/__init__.py` — new, 11 lines
- `spike/renderers/base.py` — new, 36 lines

## Checks run

- `python -c "from spike.renderers import Renderer; from spike.renderers.base import Renderer as R2; assert Renderer is R2; print('ok')"` → `ok import: spike.renderers.base`
- Used `C:\Users\arshg\AppData\Local\Programs\Python\Python313\python.exe` (see surprises).

## Surprises / notes for the human

**The venv at `spike/.venv/` is broken.** Its activation script points to `C:\Users\arshg\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe` which does not exist on this machine. Running `spike\.venv\Scripts\python.exe -c ...` produces:

```
No Python at '"C:\Users\arshg\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe'
```

For this smoke test I used the system Python 3.13 at `C:\Users\arshg\AppData\Local\Programs\Python\Python313\python.exe`, which works for plain stdlib imports. But the overnight agent's later tasks (T07 scoring → OpenCV, T11/T20 pytest tests, T13 pydantic schemas, T17 live Gemini call) will need a working venv with the project's dependencies installed.

**Recommendation for the human in the morning:** Recreate the venv before triggering live tasks:
```powershell
cd "c:\Users\arshg\AI Architecture Rendering\spike"
python -m venv .venv  # or: uv venv .venv --python 3.11
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

The overnight agent should **not** attempt to fix this — it's outside its scope (no `pip install` permitted). Tasks that depend on imports of installed third-party packages may be marked `- [!]` (blocked) by the agent until you fix the venv.

## Follow-ups

- (human, morning) Recreate `spike/.venv/` so subsequent tasks' import-checks work.
- (human, morning) Decide whether to drop a `.python-version` file or `pyproject.toml` to make the project's Python pinning explicit and reproducible.
