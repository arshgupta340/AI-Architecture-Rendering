# dependencies
node_modules/

# build output
dist/
dist-ssr/

# local env files (NEVER commit API keys — e.g. VITE_GOOGLE_MAPS_API_KEY)
.env
.env.*
!.env.example

# vite / editor cruft
*.local
.vite/
*.log
