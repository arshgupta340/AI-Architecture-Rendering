# AI Architecture Rendering — Hermes project guide

> **Source of truth:** `wiki/STATE.md` · `CLAUDE.md` · `docs/HANDOFF-web3d.md`

## Quick orientation

**Product:** Photoshop-for-Architects — web-native 3D-consistent rendering tool.
**Stack:** `apps/web3d-prototype/` (Vite + React + R3F + three r0.184, $0/client-side).
**Run:** `npm --prefix apps/web3d-prototype run dev` → http://localhost:5181

## Three render modes (all verified)
- **WebGL2** — N8AO + AgX + glass + VSM + grade + contact
- **WebGL2 + GI** — + area lights + reflective ground + contact
- **WebGPU** — SSGI + GTAO + TRAA + HDRI IBL + VSM (ceiling)

## Hero render (FLUX.1-dev + ControlNet)
- Deployed on Modal: `…--arch-rendering-flux-heroflux-web.modal.run`
- Idle = $0 (scale-to-zero), ~$0.01-0.02/render active

## Remaining polish (priority order)
1. Splat-bake deploy (`modal deploy spike/modal_splat.py`)
2. KTX2 encode for materials
3. Real cutout people
4. Multi-view hero consistency improvements
5. Atmosphere ceiling (takram clouds)

## Working with Claude Code
- Claude Code v2.1.197 at `/c/npm-global/claude`
- Auth: architect.805205@gmail.com (API account)
- Use print mode (`-p`) for one-shot tasks, tmux for interactive
- Always set `--allowedTools` to restrict capabilities
- Project has `.claude/settings.json` with permissions

## Hard rules
- **Cost cap:** $0.05/session for API calls (Modal spend user-authorized)
- **No Modal calls by default** (GPU spend is ~$0.35-0.70/run)
- **Dry-run defaults** — all driver scripts default to `--dry-run`
- **Never push without ask**
- **Spike venv:** `spike\.venv\Scripts\python.exe`
- **Wiki is source of truth** — update per CLAUDE.md § Session-log protocol
